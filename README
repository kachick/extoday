# ver 0.1.1

# ex) list next year's Mondays on 4th week of the month

. ./.sh_extoday

LANG=C; export LANG

for i in $(seq 0 365); do
    date=$(date --date "$i day" '+%Y-%m-%d')

    if [ $(show_nth_week_in_month $date) == 4 ] && [ $(show_wday $date) == Mon ]; then
        echo $date
    fi
done
